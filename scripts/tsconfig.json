//
// @naturalcycles/dev-lib/scripts/tsconfig.json
//
// Default config for /scripts
//
{
  "extends": "../cfg/tsconfig.json",
  // `include` / `exclude` need to be specified in the project tsconfig:
  "exclude": ["**/__exclude", "**/@linked"]
}
